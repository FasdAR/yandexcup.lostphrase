package ru.fasdev.yandexlostphrase

import android.content.Context
import android.graphics.Canvas
import android.util.AttributeSet
import android.util.Log
import android.view.View

class LostPhraseView : View
{
    constructor(context: Context): super(context) { init() }
    constructor(context: Context, attributeSet: AttributeSet): super(context, attributeSet) { init() }
    constructor(context: Context, attributeSet: AttributeSet, defStyleArr: Int, defStyleRes: Int): super(context, attributeSet, defStyleArr, defStyleRes) { init() }

    fun init()
    {

    }
}