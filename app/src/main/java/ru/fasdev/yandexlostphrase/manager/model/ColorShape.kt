package ru.fasdev.yandexlostphrase.manager.model

import android.graphics.Color

enum class ColorShape(val color: Int)
{
    BLACK(Color.BLACK),
    RED(Color.RED),
    WHITE(Color.WHITE),
    YELLOW(Color.YELLOW)
}